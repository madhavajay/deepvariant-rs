{
  "name": "dv-wasm-frontend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Browser dev app: drag-drop a tf.Example shard, run DeepVariant inference via dv-wasm + onnxruntime-web, download a tar.gz of predictions.",
  "scripts": {
    "build:wasm": "./build.sh",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "vite": "^7.0.0"
  },
  "dependencies": {
    "onnxruntime-web": "^1.24.0"
  }
}
